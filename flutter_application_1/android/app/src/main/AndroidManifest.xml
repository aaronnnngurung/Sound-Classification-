<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<!-- Permissions your app needs  -->

    <!-- Microphone — for sound detection (PB-01) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Camera — for flash alerts on sound detection -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera.flash" android:required="false"/>

    <!-- Notifications — for sound alerts (PB-04) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Foreground service — keeps detection running in background (PB-07) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>

    <!-- Wake lock — prevents phone from sleeping during detection -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Vibration — for haptic alerts (PB-03) -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!--
    NOTE (PB-05, smartwatch sync): no extra permissions needed here.
    The Wear OS Data Layer API (flutter_wear_os_connectivity) abstracts
    the transport away — Bluetooth/WiFi/relay is all handled internally
    by Google Play Services under ITS OWN permissions, not yours. Your
    app never touches raw Bluetooth, so there's nothing to declare.

    The two lines that WERE here (uses-feature type.watch, and the
    com.google.android.wearable.standalone meta-data below) have been
    removed — those declare "this app IS a Wear OS app" and belong only
    in the watch build flavor's manifest (android/app/src/wear/
    AndroidManifest.xml), never in the phone app's manifest. Leaving
    uses-feature type.watch here (with its default required="true")
    risked telling the Play Store your PHONE app requires Wear OS
    hardware to even install.
    -->

    <!-- Storage — for CSV download (Android 9 and below only) -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <application
        android:label="SoundClass"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:showOnLockScreen="true"
    android:turnScreenOn="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Foreground service for background sound detection (PB-07) -->
<service
    android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
    android:foregroundServiceType="microphone"
    android:exported="false"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>